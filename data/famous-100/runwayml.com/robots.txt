User-agent: *
Allow: /

# Gated assets. The landing pages at /reports/<slug> are indexable; the PDFs
# they give away are not. Each download response also carries an
# `X-Robots-Tag: noindex` header, which is the enforcing half — this rule just
# keeps well-behaved crawlers from spending requests on an endpoint that
# answers 404 without a form submission.
Disallow: /api/gated-resource/

# Hosted files (/files/<slug>, Sanity `hostedFile`) are deliberately NOT listed
# here: each is noindex or indexable per document, carried as an X-Robots-Tag on
# its own response, and a blanket Disallow would override that opt-in.

Sitemap: https://runway.com/sitemap.xml
