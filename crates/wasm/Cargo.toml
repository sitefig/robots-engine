[package]
name = "susbot-wasm"
description = "Browser bindings for the susbot robots.txt engine"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[lib]
crate-type = ["cdylib"]

[dependencies]
susbot-core = { path = "../core" }
wasm-bindgen = "0.2.128"
serde_json.workspace = true
